<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SeamlessUpdateClient" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_a100</item>
        <item name="colorPrimaryVariant">@color/blue_a100</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_a100</item>
        <item name="colorSecondaryVariant">@color/blue_a100</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <!-- Window background color. -->
        <item name="android:windowBackground">@color/black</item>
        <!-- Action bar theme. -->
        <item name="actionBarTheme">@style/ActionBar</item>
    </style>

    <!-- Action bar style. -->
    <style name="ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/black</item>
    </style>
</resources>
