<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SeamlessUpdateClient" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@*android:color/accent_device_default_dark</item>
        <item name="colorPrimaryVariant">@*android:color/accent_device_default_dark</item>
        <item name="colorOnPrimary">@android:color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@*android:color/accent_device_default_dark</item>
        <item name="colorSecondaryVariant">@*android:color/accent_device_default_dark</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <!-- Window background color. -->
        <item name="android:windowBackground">@*android:color/primary_device_default_settings</item>
        <!-- Navigation bar. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Action bar theme. -->
        <item name="actionBarTheme">@style/ActionBar</item>
    </style>

    <!-- Action bar style. -->
    <style name="ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@*android:color/primary_device_default_settings</item>
    </style>
</resources>
