<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SeamlessUpdateClient" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@*android:color/accent_device_default_light</item>
        <item name="colorPrimaryVariant">@*android:color/accent_device_default_light</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@*android:color/accent_device_default_light</item>
        <item name="colorSecondaryVariant">@*android:color/accent_device_default_light</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <!-- Status bar elements color. -->
        <item name="android:windowLightStatusBar">true</item>
        <!-- Action bar theme. -->
        <item name="actionBarTheme">@style/ActionBar</item>
        <!-- Navigation bar. -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:navigationBarDividerColor">@*android:color/navigation_bar_divider_device_default_settings</item>
        <!-- Window background color. -->
        <item name="android:windowBackground">@*android:color/background_device_default_light</item>
    </style>

    <!-- Action bar style. -->
    <style name="ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorPrimary">@android:color/transparent</item>
    </style>
</resources>
